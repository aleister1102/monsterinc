<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Title}}</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">
    {{if .EmbedAssets}}
    <style>
        /* Custom styles.css content */
        {{.StaticCSS}}
        /* CustomFontCSS if used and embedded */
        {{if .CustomFontCSS}}{{.CustomFontCSS}}{{end}}
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/2.0.3/css/dataTables.bootstrap5.min.css">
    {{else}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="assets/css/styles.css">
    {{if .CustomFontCSS}}<link rel="stylesheet" href="assets/css/custom-font.css">{{end}} {{/* Example if custom font is linked */}}
    {{end}}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">MonsterInc Scan Report</a>
            <span class="navbar-text ms-auto">Generated: {{.Timestamp}}</span>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
                <div class="position-sticky pt-3">
                    <h5>Targets</h5>
                    <ul class="nav flex-column" id="target-list">
                        <li class="nav-item"><a class="nav-link active" href="#" data-target="all">All Targets</a></li>
                        {{range .RootTargets}}
                        <li class="nav-item"><a class="nav-link" href="#{{.}}" data-target="{{.}}">{{.}}</a></li>
                        {{end}}
                    </ul>
                    <hr>
                    <h5>Filters</h5>
                    <div class="mb-3">
                        <label for="statusCodeFilter" class="form-label">Status Code</label>
                        <select class="form-select form-select-sm" id="statusCodeFilter"><option value="">All</option></select>
                    </div>
                    <div class="mb-3">
                        <label for="contentTypeFilter" class="form-label">Content Type</label>
                        <select class="form-select form-select-sm" id="contentTypeFilter"><option value="">All</option></select>
                    </div>
                     <div class="mb-3">
                        <label for="techFilter" class="form-label">Technologies</label>
                        <input type="text" class="form-control form-control-sm" id="techFilter" placeholder="e.g., nginx, react">
                    </div>
                </div>
            </nav>

            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2">Scan Results <span id="currentFilterSummary" class="fs-6 fw-normal"></span></h1>
                    <input type="text" id="globalSearch" class="form-control form-control-sm w-auto" placeholder="Search report...">
                </div>

                <div class="table-responsive">
                    <table id="resultsTable" class="table table-striped table-hover table-sm">
                        <thead>
                            <tr>
                                {{range .Headers}}
                                <th scope="col" class="sortable" data-column-name="{{.|lower|replace " " "-"}}">{{.}}</th>
                                {{end}}
                            </tr>
                        </thead>
                        <tbody>
                            <tr><td colspan="{{len .Headers}}" class="text-center">Loading...</td></tr>
                        </tbody>
                    </table>
                </div>
                <nav aria-label="Page navigation"><ul class="pagination justify-content-center" id="pagination-controls"></ul></nav>
            </main>
        </div>
    </div>
    
    <script>
        window.reportSettings = {
            itemsPerPage: {{.ItemsPerPage}},
            initialProbeResults: JSON.parse('{{.ProbeResultsJSON}}')
        };
    </script>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script src="https://cdn.datatables.net/2.0.3/js/dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/2.0.3/js/dataTables.bootstrap5.min.js"></script>

    {{if .EmbedAssets}}
    {{/* DataTablesJS is no longer embedded via Go; StaticJS is for custom report.js */}}
    <script>{{.StaticJS}}</script>
    {{else}}
    {{/* DataTablesJS is no longer embedded via Go; StaticJS is for custom report.js */}}
    <script src="assets/js/report.js"></script>
    {{end}}
</body>
</html> 